name: Build APK

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Extract ZIP
        run: |
          unzip -o jmovies.zip
          if [ ! -f "pubspec.yaml" ]; then
            DIR=$(find . -maxdepth 2 -name "pubspec.yaml" -exec dirname {} \;)
            if [ -n "$DIR" ]; then
              cp -r $DIR/* .
            fi
          fi

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Recreate Clean Android Project
        run: |
          rm -rf android
          flutter create . --platforms=android --org com.jmovies.app
          mkdir -p assets/icons assets/images
          sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"\/>\n    <application android:usesCleartextTraffic="true" android:hardwareAccelerated="true"/' android/app/src/main/AndroidManifest.xml

      - name: Add Dependencies (Carousel and WebView)
        run: |
          flutter pub add carousel_slider:^5.0.0
          flutter pub add webview_flutter
          flutter pub get

      - name: Setup Tokens & .env File
        env:
          TMDB_SECRET: ${{ secrets.TMDB_READ_TOKEN }}
        run: |
          KEY="dac07662083decf2616c4e68d22342c9"
          TOKEN="${TMDB_SECRET:-eyJhbGciOiJIUzI1NiJ9.eyJhdWQiOiJkYWMwNzY2MjA4M2RlY2YyNjE2YzRlNjhkMjIzNDJjOSIsIm5iZiI6MTc4OTI5NDQ3My45NDgsInN1YiI6IjZhYTY3Nzg5YjFmNzgyMjZjYjU3ODAyNSIsInNjb3BlcyI6WyJhcGlfcmVhZCJdLCJ2ZXJzaW9uIjoxfQ.nXRm_2AAufGAeHFBuMLf4Dg5yViQ-jlTEF2CkYp6WL4}"

          cat << EOF > .env
          TMDB_ACCESS_TOKEN=$TOKEN
          TMDB_READ_TOKEN=$TOKEN
          TMDB_TOKEN=$TOKEN
          ACCESS_TOKEN=$TOKEN
          TOKEN=$TOKEN
          TMDB_API_KEY=$KEY
          API_KEY=$KEY
          TMDB_KEY=$KEY
          BASE_URL=https://api.themoviedb.org/3
          IMAGE_BASE_URL=https://image.tmdb.org/t/p/w500
          EOF

          cp .env assets/.env

          if ! grep -q "\.env" pubspec.yaml; then
            sed -i '/flutter:/a \  assets:\n    - .env\n    - assets/\n    - assets/images/\n    - assets/icons/' pubspec.yaml
          fi

      - name: Smart Key and Bearer Token Injection
        env:
          TMDB_SECRET: ${{ secrets.TMDB_READ_TOKEN }}
        run: |
          export KEY="dac07662083decf2616c4e68d22342c9"
          export TOKEN="${TMDB_SECRET:-eyJhbGciOiJIUzI1NiJ9.eyJhdWQiOiJkYWMwNzY2MjA4M2RlY2YyNjE2YzRlNjhkMjIzNDJjOSIsIm5iZiI6MTc4OTI5NDQ3My45NDgsInN1YiI6IjZhYTY3Nzg5YjFmNzgyMjZjYjU3ODAyNSIsInNjb3BlcyI6WyJhcGlfcmVhZCJdLCJ2ZXJzaW9uIjoxfQ.nXRm_2AAufGAeHFBuMLf4Dg5yViQ-jlTEF2CkYp6WL4}"

          python3 - << 'EOF'
          import os, re

          key = os.environ.get("KEY", "")
          token = os.environ.get("TOKEN", "")

          for root, _, files in os.walk("lib"):
              for f in files:
                  if f.endswith(".dart"):
                      p = os.path.join(root, f)
                      with open(p, "r", encoding="utf-8", errors="ignore") as file:
                          content = file.read()

                      def replace_dotenv(m):
                          var = m.group(1).upper()
                          if "TOKEN" in var or "AUTH" in var:
                              return f"'{token}'"
                          return f"'{key}'"

                      content = re.sub(r"dotenv\.(?:env\[|get\()[\'\"]([^\'\"]+)[\'\"](?:\]|\))", replace_dotenv, content)

                      content = content.replace("YOUR_TMDB_API_KEY", key).replace("YOUR_API_KEY", key).replace("your_api_key_here", key)
                      content = content.replace("YOUR_READ_ACCESS_TOKEN", token).replace("YOUR_ACCESS_TOKEN", token).replace("YOUR_TOKEN", token)

                      content = re.sub(r'([a-zA-Z0-9_]*token[a-zA-Z0-9_]*\s*=\s*)[\'\"][^\'\"]*[\'\"]', rf"\g<1>'{token}'", content, flags=re.IGNORECASE)
                      content = re.sub(r'([a-zA-Z0-9_]*bearer[a-zA-Z0-9_]*\s*=\s*)[\'\"][^\'\"]*[\'\"]', rf"\g<1>'{token}'", content, flags=re.IGNORECASE)

                      def replace_key(m):
                          var = m.group(1)
                          if "token" in var.lower():
                              return f"{var}'{token}'"
                          return f"{var}'{key}'"

                      content = re.sub(r'([a-zA-Z0-9_]*key[a-zA-Z0-9_]*\s*=\s*)[\'\"][^\'\"]*[\'\"]', replace_key, content, flags=re.IGNORECASE)

                      content = content.replace(f"Bearer {key}", f"Bearer {token}")
                      content = re.sub(r'Duration\(\s*seconds\s*:\s*[0-9]+\s*\)', 'Duration(seconds: 60)', content)

                      with open(p, "w", encoding="utf-8") as file:
                          file.write(content)
          EOF

      - name: Inject Live WebView Streaming Player
        run: |
          cat << 'EOF' > lib/screens/player/player_screen.dart
          import 'dart:async';
          import 'package:flutter/material.dart';
          import 'package:flutter/services.dart';
          import 'package:wakelock_plus/wakelock_plus.dart';
          import 'package:webview_flutter/webview_flutter.dart';
          import '../../data/models/movie_model.dart';

          class PlayerScreen extends StatefulWidget {
            final Movie movie;
            const PlayerScreen({super.key, required this.movie});

            @override
            State<PlayerScreen> createState() => _PlayerScreenState();
          }

          class _PlayerScreenState extends State<PlayerScreen> {
            late final WebViewController _controller;
            bool _isLoading = true;
            int _currentServerIndex = 0;

            late final List<Map<String, String>> _servers;

            @override
            void initState() {
              super.initState();
              SystemChrome.setPreferredOrientations([
                DeviceOrientation.landscapeLeft,
                DeviceOrientation.landscapeRight,
              ]);
              SystemChrome.setEnabledSystemUIMode(SystemUiMode.immersiveSticky);
              WakelockPlus.enable();

              final id = widget.movie.id;
              _servers = [
                {
                  'name': 'Server Alpha (AutoEmbed)',
                  'url': 'https://player.autoembed.cc/embed/movie/$id',
                },
                {
                  'name': 'Server Beta (VidLink)',
                  'url': 'https://vidlink.pro/movie/$id',
                },
                {
                  'name': 'Server Gamma (VidSrc)',
                  'url': 'https://vidsrc.cc/v2/embed/movie/$id',
                },
                {
                  'name': 'Server Delta (MultiEmbed)',
                  'url': 'https://multiembed.mov/?video_id=$id&tmdb=1',
                },
              ];

              _initWebView();
            }

            void _initWebView() {
              _controller = WebViewController()
                ..setJavaScriptMode(JavaScriptMode.unrestricted)
                ..setBackgroundColor(Colors.black)
                ..setNavigationDelegate(
                  NavigationDelegate(
                    onPageStarted: (String url) {
                      if (mounted) setState(() => _isLoading = true);
                    },
                    onPageFinished: (String url) {
                      if (mounted) setState(() => _isLoading = false);
                    },
                    onNavigationRequest: (NavigationRequest request) {
                      final url = request.url.toLowerCase();
                      if (url.startsWith('blob:') ||
                          url.startsWith('about:') ||
                          url.contains('autoembed') ||
                          url.contains('vidlink') ||
                          url.contains('vidsrc') ||
                          url.contains('multiembed') ||
                          url.contains('embed') ||
                          url.contains('stream') ||
                          url.contains('m3u8') ||
                          url.contains('mp4') ||
                          url.contains('cloud') ||
                          url.contains('player')) {
                        return NavigationDecision.navigate;
                      }
                      return NavigationDecision.prevent;
                    },
                  ),
                )
                ..loadRequest(Uri.parse(_servers[_currentServerIndex]['url']!));
            }

            void _switchServer(int index) {
              if (index == _currentServerIndex) return;
              setState(() {
                _currentServerIndex = index;
                _isLoading = true;
              });
              _controller.loadRequest(Uri.parse(_servers[index]['url']!));
            }

            void _showServerSheet() {
              showModalBottomSheet(
                context: context,
                backgroundColor: const Color(0xFF1E1E1E),
                shape: const RoundedRectangleBorder(
                  borderRadius: BorderRadius.vertical(top: Radius.circular(16)),
                ),
                builder: (context) {
                  return SafeArea(
                    child: Column(
                      mainAxisSize: MainAxisSize.min,
                      children: [
                        const Padding(
                          padding: EdgeInsets.all(16),
                          child: Text(
                            'Select Server',
                            style: TextStyle(
                              color: Colors.white,
                              fontSize: 18,
                              fontWeight: FontWeight.bold,
                            ),
                          ),
                        ),
                        ..._servers.asMap().entries.map((entry) {
                          final idx = entry.key;
                          final server = entry.value;
                          final isSelected = idx == _currentServerIndex;
                          return ListTile(
                            leading: Icon(
                              isSelected ? Icons.play_circle_fill : Icons.dns_rounded,
                              color: isSelected ? Colors.redAccent : Colors.white70,
                            ),
                            title: Text(
                              server['name']!,
                              style: TextStyle(
                                color: isSelected ? Colors.redAccent : Colors.white,
                                fontWeight: isSelected ? FontWeight.bold : FontWeight.normal,
                              ),
                            ),
                            trailing: isSelected
                                ? const Icon(Icons.check, color: Colors.redAccent)
                                : null,
                            onTap: () {
                              Navigator.pop(context);
                              _switchServer(idx);
                            },
                          );
                        }),
                        const SizedBox(height: 12),
                      ],
                    ),
                  );
                },
              );
            }

            @override
            void dispose() {
              SystemChrome.setPreferredOrientations([DeviceOrientation.portraitUp]);
              SystemChrome.setEnabledSystemUIMode(SystemUiMode.edgeToEdge);
              WakelockPlus.disable();
              super.dispose();
            }

            @override
            Widget build(BuildContext context) {
              return Scaffold(
                backgroundColor: Colors.black,
                body: SafeArea(
                  child: Stack(
                    children: [
                      WebViewWidget(controller: _controller),
                      if (_isLoading)
                        Container(
                          color: Colors.black,
                          child: Center(
                            child: Column(
                              mainAxisSize: MainAxisSize.min,
                              children: [
                                const CircularProgressIndicator(color: Colors.redAccent),
                                const SizedBox(height: 16),
                                Text(
                                  'Connecting to stream (${_servers[_currentServerIndex]['name']})...',
                                  style: const TextStyle(color: Colors.white70, fontSize: 14),
                                ),
                              ],
                            ),
                          ),
                        ),
                      Positioned(
                        top: 12,
                        left: 12,
                        right: 12,
                        child: Row(
                          children: [
                            Container(
                              decoration: BoxDecoration(
                                color: Colors.black.withOpacity(0.65),
                                shape: BoxShape.circle,
                              ),
                              child: IconButton(
                                icon: const Icon(Icons.arrow_back_ios_new, color: Colors.white, size: 20),
                                onPressed: () => Navigator.pop(context),
                              ),
                            ),
                            const SizedBox(width: 12),
                            Expanded(
                              child: Text(
                                widget.movie.title,
                                style: const TextStyle(
                                  color: Colors.white,
                                  fontSize: 16,
                                  fontWeight: FontWeight.bold,
                                  shadows: [
                                    Shadow(blurRadius: 4, color: Colors.black),
                                  ],
                                ),
                                overflow: TextOverflow.ellipsis,
                              ),
                            ),
                            Container(
                              decoration: BoxDecoration(
                                color: Colors.black.withOpacity(0.65),
                                borderRadius: BorderRadius.circular(20),
                              ),
                              child: TextButton.icon(
                                onPressed: _showServerSheet,
                                icon: const Icon(Icons.dns_rounded, color: Colors.white, size: 16),
                                label: Text(
                                  'Server ${_currentServerIndex + 1}',
                                  style: const TextStyle(color: Colors.white, fontSize: 13),
                                ),
                              ),
                            ),
                          ],
                        ),
                      ),
                    ],
                  ),
                ),
              );
            }
          }
          EOF

      - name: Patch Plugins to Android SDK 34
        run: |
          find $HOME/.pub-cache -name "build.gradle" -exec sed -i -E 's/compileSdkVersion[ =]+[0-9]+/compileSdkVersion 34/g' {} + 2>/dev/null || true
          find $HOME/.pub-cache -name "build.gradle" -exec sed -i -E 's/compileSdk[ =]+[0-9]+/compileSdk 34/g' {} + 2>/dev/null || true
          if [ -f "android/build.gradle" ]; then
            cat << 'EOF' >> android/build.gradle

          subprojects {
              afterEvaluate { project ->
                  if (project.hasProperty('android')) {
                      project.android {
                          compileSdkVersion 34
                      }
                  }
              }
          }
          EOF
          fi

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: jmovies-apk
          path: build/app/outputs/flutter-apk/*.apk
